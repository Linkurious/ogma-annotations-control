# Function: isHexColor()

```ts
function isHexColor(color): color is `#${string}`;
```

Type guard to check if a string is a valid hex color

## Parameters

### color

`string`

## Returns

`` color is `#${string}` ``
