# Function: hexToRgba()

```ts
function hexToRgba(color, alpha): RgbaColor;
```

Adds alpha channel to a hex color

## Parameters

### color

`` `#${string}` ``

### alpha

`number`

## Returns

[`RgbaColor`](../type-aliases/RgbaColor.md)

rgba color string
