# Variable: defaultArrowOptions

```ts
const defaultArrowOptions: Arrow;
```

Default options for creating new Arrow annotations.
Contains the default arrow structure with [defaultArrowStyle](defaultArrowStyle.md).
