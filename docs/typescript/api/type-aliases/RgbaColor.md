# Type Alias: RgbaColor

```ts
type RgbaColor = 
  | `rgba(${number}, ${number}, ${number}, ${number})`
  | `rgba(${number},${number},${number},${number})`;
```

RGBA color string in format rgba(r, g, b, a)

## Example

```ts
"rgba(255, 0, 0, 1)" | "rgba(128, 128, 128, 0.5)"
```
