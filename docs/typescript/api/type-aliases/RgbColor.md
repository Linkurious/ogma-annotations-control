# Type Alias: RgbColor

```ts
type RgbColor = 
  | `rgb(${number}, ${number}, ${number})`
  | `rgb(${number},${number},${number})`;
```

RGB color string in format rgb(r, g, b)

## Example

```ts
"rgb(255, 0, 0)" | "rgb(128, 128, 128)"
```
