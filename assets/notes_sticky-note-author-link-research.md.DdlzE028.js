import{_ as t,o,c as s,a2 as i}from"./chunks/framework.CFryVJGt.js";const u=JSON.parse('{"title":"Research: adding an author/link sub-section to Sticky Notes","description":"","frontmatter":{},"headers":[],"relativePath":"notes/sticky-note-author-link-research.md","filePath":"notes/sticky-note-author-link-research.md"}'),n={name:"notes/sticky-note-author-link-research.md"};function a(c,e,d,r,l,h){return o(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="research-adding-an-author-link-sub-section-to-sticky-notes" tabindex="-1">Research: adding an author/link sub-section to Sticky Notes <a class="header-anchor" href="#research-adding-an-author-link-sub-section-to-sticky-notes" aria-label="Permalink to &quot;Research: adding an author/link sub-section to Sticky Notes&quot;">​</a></h1><p>Fact-finding only — no design proposal. Written for a design discussion about adding a &quot;sub-section with author info etc.&quot; to Sticky Notes, with a free-form link at the bottom of the card, possibly shown only when the note is selected.</p><p>All citations are <code>file:line</code> in this repo unless noted otherwise.</p><h2 id="_1-sticky-note-is-not-a-distinct-data-type-—-it-s-a-styled-text-annotation" tabindex="-1">1. Sticky Note is not a distinct data type — it&#39;s a styled <code>Text</code> annotation <a class="header-anchor" href="#_1-sticky-note-is-not-a-distinct-data-type-—-it-s-a-styled-text-annotation" aria-label="Permalink to &quot;1. Sticky Note is not a distinct data type — it&#39;s a styled \`Text\` annotation&quot;">​</a></h2><p>There is no <code>StickyNote</code> type, class, or properties interface anywhere in the codebase. &quot;Sticky note&quot; is a <em>drawing-mode preset</em>: it creates a regular <code>Text</code> annotation (<code>type: &quot;text&quot;</code>) with Miro-style default styling and no connector arrow.</p><ul><li><code>packages/core/src/api/drawing.ts:31-52</code> — <code>STICKY_NOTE_SIZE = 160</code> and <code>defaultStickyNoteStyle</code> (yellow <code>#FFEB99</code> background, <code>IBM Plex Sans</code>, <code>placeholder: &quot;Quick note…&quot;</code>, <code>fixedSize: false</code>).</li><li><code>packages/core/src/api/drawing.ts:262-313</code> — <code>enableStickyNoteDrawing()</code> / <code>startStickyNote()</code> call <code>createText(x, y, 0, 0, &quot;&quot;, { ...defaultStickyNoteStyle, ...style })</code> and note in comments: <em>&quot;It&#39;s a regular <code>text</code> annotation, so it goes through the same interactive corner-drag <code>TextHandler.startDrawing()</code> as <code>startBox</code>/<code>startText</code>&quot;</em>.</li><li><code>packages/core/src/Control.ts:510-546</code> — public API doc for <code>enableStickyNoteDrawing</code>, same statement: <em>&quot;It&#39;s a regular <code>text</code> annotation, unlike <code>enableCommentDrawing</code>.&quot;</em></li><li>Data model: <code>packages/core/src/types/features/Text.ts:33-48</code> — <code>TextProperties</code> (<code>type: &quot;text&quot;</code>, <code>content</code>, <code>width</code>, <code>height</code>, <code>style</code>) and <code>Text extends AnnotationFeature&lt;GeoJSONPoint, TextProperties&gt;</code>. No author/id/link fields beyond the base <code>AnnotationFeature</code> (<code>id</code>, GeoJSON <code>Feature</code> shape) — see <code>packages/core/src/types/features/Annotation.ts:9-30</code>.</li><li>UI entry points that reference &quot;sticky note&quot; as a toolbar mode name only (not a data type): <code>packages/core/src/ui/AnnotationToolbar.ts:23,34,42,254-263</code>, <code>packages/core/src/ui/icons.ts:32,67</code>, <code>packages/react/src/ui/AddMenu.tsx:17,45,149-154,204-210</code>.</li><li>e2e coverage exists at <code>packages/core/test/e2e/stickyNote.test.ts</code> (behavior contract for placement/selection/editing), useful as a reference for what must keep working.</li></ul><p><strong>Open question</strong>: extending &quot;sticky notes&quot; specifically (vs. all <code>Text</code> annotations, since they&#39;re the same type) means either (a) adding the field to <code>TextProperties</code> generally, which would also affect boxes/plain text, or (b) inventing a way to distinguish &quot;this text annotation is a sticky note&quot; after creation (there is currently no such marker/flag persisted on the feature — the distinction only exists at creation time via the style preset).</p><h2 id="_2-author-timestamp-fields-already-exist-—-but-only-on-comment-a-different-feature-type" tabindex="-1">2. <code>author</code>/<code>timestamp</code> fields already exist — but only on <code>Comment</code>, a different feature type <a class="header-anchor" href="#_2-author-timestamp-fields-already-exist-—-but-only-on-comment-a-different-feature-type" aria-label="Permalink to &quot;2. \`author\`/\`timestamp\` fields already exist — but only on \`Comment\`, a different feature type&quot;">​</a></h2><p><code>Comment</code> (the chat-bubble, arrow-anchored callout — a separate annotation type from Text/sticky note) already has optional metadata fields:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// packages/core/src/types/features/Comment.ts:71-93</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CommentProps</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AnnotationProps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;comment&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  content</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ...;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  width</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  height</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Optional metadata */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  author</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  timestamp</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  style</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CommentStyle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li>Populated (optionally, both <code>undefined</code> by default) in <code>createComment()</code>: <code>packages/core/src/types/features/Comment.ts:239-251</code>.</li><li>Neither field is read anywhere else in <code>packages/core/src</code> or <code>packages/react/src</code> (no renderer, panel, or handler references <code>.properties.author</code> or <code>.properties.timestamp</code> — confirmed via repo-wide grep). They are typed but currently <strong>unused/undisplayed</strong> — i.e. plumbing exists but no UI surfaces them today, even on Comment.</li><li>No <code>createdAt</code>/<code>updatedAt</code> or any other author-like concept exists anywhere else in the codebase (checked across <code>packages/core/src</code> and <code>packages/react/src</code>, excluding tests).</li></ul><p><strong>Implication for the customer&#39;s ask</strong>: since Sticky Note = <code>Text</code>, not <code>Comment</code>, this existing <code>author</code>/<code>timestamp</code> shape isn&#39;t currently reachable by sticky notes at all. It&#39;s a precedent for <em>where such fields would go</em> (base <code>*Props</code> interface, optional, <code>?:</code>) but not something sticky notes inherit automatically.</p><h2 id="_3-selection-state-and-how-rendering-differs-when-selected" tabindex="-1">3. Selection state and how rendering differs when selected <a class="header-anchor" href="#_3-selection-state-and-how-rendering-differs-when-selected" aria-label="Permalink to &quot;3. Selection state and how rendering differs when selected&quot;">​</a></h2><p>Selection lives in a single Zustand-style store, not per-component React state:</p><ul><li><code>packages/core/src/store/index.ts:87</code> — <code>selectedFeatures: Set&lt;Id&gt;</code> on <code>AnnotationState</code>.</li><li>Mutators: <code>setSelectedFeatures</code>, <code>select</code>, <code>unselect</code>, <code>clearSelection</code>, <code>isSelected</code> — <code>packages/core/src/store/index.ts:428-457</code>.</li><li>The SVG renderer subscribes to <code>selectedFeatures</code> (among other slices) and re-renders on change: <code>packages/core/src/renderer/shapes/index.ts:56,68,82,164,219</code>.</li></ul><p><strong>Existing precedent for &quot;show X only when selected&quot; at the shape level</strong> (the closest analog to the requested behavior) is on <code>Comment</code>, not <code>Text</code>: an edit-button badge is computed and rendered conditionally based on selection:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// packages/core/src/renderer/shapes/comment.ts:446-450</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> showEditBtn</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  mode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> COMMENT_MODE_COLLAPSED</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  state.selectedFeatures.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">has</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(annotation.id) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  state.editingFeature </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> annotation.id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  state.options.showEditButton;</span></span></code></pre></div><p>This badge is a small <code>foreignObject</code> positioned in the corner of the comment box (<code>packages/core/src/renderer/shapes/comment.ts:339-370</code>), added to the group only <code>if (showEditBtn)</code>.</p><p>The <code>Text</code> renderer (<code>packages/core/src/renderer/shapes/text.ts</code>) has <strong>no equivalent conditional content today</strong> — it always renders the same <code>&lt;rect&gt;</code> + <code>&lt;text&gt;/&lt;tspan&gt;</code> content regardless of selection state; only the background gets a hover-brighten effect (<code>text.ts:70-76</code>). Resize/drag handles for a selected shape are drawn by a separate renderer (<code>packages/core/src/renderer/handles.ts:50</code>, subscribes to <code>selectedFeatures</code> too), not by <code>renderText</code>.</p><p><strong>Off-canvas alternative</strong>: there is a second place that already reveals UI &quot;only when selected&quot; — the <code>AnnotationPanel</code> (a plain HTML side panel, not part of the on-canvas SVG card):</p><ul><li>Visibility state machine: <code>packages/core/src/ui/panelVisibility.ts</code> — <code>attachPanelVisibility()</code> shows the panel ~150ms after a single-annotation <code>select</code> event (long enough to distinguish a click from a drag-start, which cancels it) and hides on <code>unselect</code>/multi-select (<code>panelVisibility.ts:47-127</code>).</li><li><code>packages/core/src/ui/AnnotationPanel.ts:120-137</code> — <code>setAnnotation()</code> routes <code>isText(...) || isBox(...) || isComment(...)</code> to the same <code>renderText()</code> panel layout (<code>AnnotationPanel.ts:161-182</code>), i.e. the panel already treats sticky notes, plain text, boxes, and comments uniformly.</li><li>Panel content is built as HTML strings via a <code>section(title, content)</code> helper (<code>AnnotationPanel.ts:199-201</code>) and fully replaces <code>this.panelBody.innerHTML</code> on every selection change — a new &quot;Author&quot; section would slot in as one more <code>\${this.section(&quot;Author&quot;, ...)}</code> call in <code>renderText()</code>.</li><li>Caveat: this panel currently only edits <strong>style</strong> (calls <code>control.updateStyle(...)</code>, see doc <code>docs/typescript/ui-components/style-panel.md:56-58</code> — <em>&quot;Editing a control calls <code>control.updateStyle(...)</code>&quot;</em>); it has no existing path for editing arbitrary content/metadata properties like an author name or URL, only style properties (color, background, font, stroke).</li></ul><h2 id="_4-card-content-footer-layout-today-on-canvas-svg" tabindex="-1">4. Card content/footer layout today (on-canvas SVG) <a class="header-anchor" href="#_4-card-content-footer-layout-today-on-canvas-svg" aria-label="Permalink to &quot;4. Card content/footer layout today (on-canvas SVG)&quot;">​</a></h2><p>The on-canvas &quot;card&quot; is <strong>pure SVG</strong>, built imperatively in <code>packages/core/src/renderer/shapes/text.ts</code> — not an HTML/DOM element:</p><ul><li><code>renderText()</code> (<code>text.ts:17-94</code>) creates one <code>&lt;g class=&quot;annotation-text&quot;&gt;</code> containing a single <code>&lt;rect&gt;</code> (background/border) and the text content.</li><li>Content is laid out by <code>drawContent()</code> (<code>text.ts:154-243</code>): measures/wraps text with <code>@chenglou/pretext</code>, then emits one <code>&lt;text&gt;</code> with one <code>&lt;tspan&gt;</code> per visible line, positioned by explicit <code>x</code>/<code>dy</code> offsets computed from <code>padding</code> and line height. There is <strong>no header/body/footer sectioning</strong> — it&#39;s a single text block that fills the box from the top, truncated with <code>…</code> if it overflows <code>maxHeight</code>/line count (<code>text.ts:185-194</code>).</li><li>There is no <code>foreignObject</code> in the Text renderer (unlike <code>Comment</code>, see below), so no CSS flexbox/grid &quot;footer&quot; area exists to slot new content into — adding one (e.g., a bottom-pinned link row) would mean either (a) more SVG <code>&lt;text&gt;</code>/<code>&lt;tspan&gt;</code> elements with manually computed y-offsets anchored to the box&#39;s bottom edge, similar to how the rect&#39;s <code>height</code> is used to place things, or (b) introducing a <code>foreignObject</code> into the Text renderer for the first time, as <code>Comment</code> already does.</li><li><code>Comment</code>&#39;s box, by contrast, <em>does</em> use a <code>foreignObject</code> for its content (<code>packages/core/src/renderer/shapes/comment.ts:310-337</code>) with a real HTML <code>&lt;div&gt;</code> (padding, line-height, overflow, <code>pointer-events</code> toggling so clicks pass through to the background <code>&lt;rect&gt;</code> except on links/buttons). This is the more natural existing pattern to extend for an HTML-laid-out footer sub-section, and it&#39;s already selection-gated for the edit button (§3).</li></ul><h2 id="_5-existing-link-url-patterns" tabindex="-1">5. Existing &quot;link&quot;/URL patterns <a class="header-anchor" href="#_5-existing-link-url-patterns" aria-label="Permalink to &quot;5. Existing &quot;link&quot;/URL patterns&quot;">​</a></h2><p>Two unrelated concepts both use the word &quot;link&quot; in this codebase — worth flagging to avoid confusion in the design discussion:</p><p><strong>(a) Auto-detected URLs inside annotation text</strong> — the closest existing precedent for a &quot;free-form link&quot;:</p><ul><li><code>packages/core/src/utils/rendering.ts:7,39-41</code> — <code>ANNOTATION_LINK_CLASS = &quot;ogma-annotation-link&quot;</code> and <code>createUrlPattern()</code> returning <code>/(https?:\\/\\/[^\\s&lt;]+)/g</code>.</li><li>Used in the <code>Text</code> renderer to turn any <code>http(s)://…</code> substring typed into the content into a clickable SVG <code>&lt;a&gt;</code> inside a <code>&lt;tspan&gt;</code>: <code>packages/core/src/renderer/shapes/text.ts:213-231</code>.</li><li>Used in the <code>Comment</code> renderer&#39;s <code>formatContent()</code> to do the same via HTML string replacement into the <code>foreignObject</code>&#39;s <code>&lt;div&gt;</code>: <code>packages/core/src/renderer/shapes/comment.ts:384-403</code>.</li><li><code>packages/core/src/utils/rendering.ts:9-25</code> — <code>isAnnotationLinkTarget()</code> lets click handlers detect a click landed on one of these auto-links and skip selection/edit-mode entry instead (<code>packages/core/src/interaction/index.ts</code> uses this pattern; confirmed via the <code>.ogma-annotation-link</code> class name shared between renderer and interaction code).</li><li>This is <strong>auto-detection inside free-typed body text</strong>, not a discrete &quot;link&quot; field on the annotation&#39;s properties. A &quot;free-form link at the bottom of the card&quot; as a dedicated field would be new: no annotation type currently has a structured <code>url</code>/<code>link</code> string property.</li></ul><p><strong>(b) <code>Link</code>/<code>ExportedLink</code> — an unrelated, pre-existing concept</strong>: structural attachment of an <em>arrow</em> to another annotation or graph node/edge, stored on <code>arrow.properties.link</code>. Nothing to do with URLs.</p><ul><li><code>packages/core/src/types/features/Link.ts:33-63</code> — <code>Link</code> (<code>arrow</code>, <code>id</code>, <code>side</code>, <code>target</code>, <code>targetType</code>, <code>magnet</code>) and <code>ExportedLink</code> (serialized form).</li><li>Documented in <code>docs/typescript/core-concepts/annotations.md:250-351</code> (&quot;How Links Work&quot;, &quot;Creating Links&quot;, etc.) — entirely about arrow connectors, e.g. <em>&quot;Links are stored in the <code>arrow.properties.link</code> property&quot;</em> (<code>annotations.md:267</code>).</li></ul><p><strong>Naming risk</strong>: calling the new sticky-note feature a &quot;link&quot; field risks colliding with this existing <code>Link</code>/<code>link</code> vocabulary that means something structurally different (arrow attachment) elsewhere in the same codebase and docs.</p><h2 id="_6-serialization-schema-conventions" tabindex="-1">6. Serialization / schema conventions <a class="header-anchor" href="#_6-serialization-schema-conventions" aria-label="Permalink to &quot;6. Serialization / schema conventions&quot;">​</a></h2><ul><li>Annotations serialize as plain GeoJSON <code>Feature</code>s — no custom envelope, no schema-version field found anywhere in <code>packages/core/src</code> or <code>packages/react/src</code>. Confirmed via repo-wide search for <code>createdAt|updatedAt|schema.*version</code> (only hits were an unrelated <code>updatedAttributes</code> parameter in <code>packages/core/src/handlers/links/sync.ts:169-174</code>, not a schema version).</li><li><code>docs/AI_REFERENCE_TYPESCRIPT.md:665</code> — <em>&quot;All annotations are based on the GeoJSON specification..., making them easy to serialize, store, and share.&quot;</em></li><li>No migration helper, versioning field, or upgrade path exists for annotation shapes in this codebase today. Adding a new optional property (e.g. <code>link?: string</code>, <code>author?: string</code>) to <code>TextProperties</code> would be a purely additive, backward-compatible change under this convention (old saved annotations simply lack the field) — but there is no established <em>process</em> (e.g. a version bump, a migrate-on-load step) for such changes; this would be new territory, not something to follow an existing pattern for.</li></ul><h2 id="_7-where-research-design-notes-live-in-this-repo" tabindex="-1">7. Where research/design notes live in this repo <a class="header-anchor" href="#_7-where-research-design-notes-live-in-this-repo" aria-label="Permalink to &quot;7. Where research/design notes live in this repo&quot;">​</a></h2><p>No existing <code>notes/</code>, <code>ADR/</code>, or research-doc folder was found anywhere in the repo (checked for <code>ADR</code>, <code>RFC</code>, <code>research</code> in file/dir names, and for a <code>CONTRIBUTING.md</code> — none exist). <code>docs/</code> is a published VitePress documentation site (<code>docs/.vitepress/</code>, <code>docs/typescript/</code>, <code>docs/react/</code>, <code>docs/architecture/</code> for diagrams) rather than a place for working notes — its content is user-facing reference material, generated from JSDoc in part (<code>docs/typescript/api/**</code>).</p><p>Since there&#39;s no established convention, this file was placed at <code>docs/notes/sticky-note-author-link-research.md</code>, alongside (but clearly separated from) the published docs, following the existing <code>docs/&lt;topic&gt;/</code> sub-folder pattern used elsewhere (<code>docs/architecture/</code>, <code>docs/typescript/</code>). If the team has a different preferred location (e.g. outside <code>docs/</code> entirely so it doesn&#39;t ship with the VitePress build), this should move.</p><h2 id="summary-of-open-questions-for-the-design-discussion" tabindex="-1">Summary of open questions for the design discussion <a class="header-anchor" href="#summary-of-open-questions-for-the-design-discussion" aria-label="Permalink to &quot;Summary of open questions for the design discussion&quot;">​</a></h2><ul><li>Sticky Note = <code>Text</code> annotation with a style preset, not a distinct type — should the new fields go on <code>TextProperties</code> generally (affecting boxes/plain text too), or does &quot;sticky note&quot; need to become a distinguishable/persisted sub-type first?</li><li><code>Comment</code> already has unused <code>author?</code>/<code>timestamp?</code> fields with no reader anywhere — worth deciding whether to finally wire those up as the model for the new fields, or design fresh.</li><li>On-canvas <code>Text</code> rendering is pure SVG with no footer/section concept and no <code>foreignObject</code> today; <code>Comment</code>&#39;s <code>foreignObject</code> + selection-gated edit-button badge is the closest existing pattern for &quot;extra chrome shown only when selected.&quot;</li><li>The off-canvas <code>AnnotationPanel</code> already shows/hides on selection and already unifies Text/Box/Comment, but it currently only edits <em>style</em>, not content/metadata — extending it to author/link would be new capability for that component, not just a new section.</li><li>&quot;Link&quot; as a name collides with the pre-existing <code>arrow.properties.link</code> (attachment) concept; the closest existing &quot;link&quot; behavior is auto-detected URLs inside typed content, not a discrete field.</li><li>No schema-versioning/migration convention exists in this codebase; adding fields is additive/backward-compatible by default but there&#39;s no process to model against.</li></ul>`,38)])])}const g=t(n,[["render",a]]);export{u as __pageData,g as default};
